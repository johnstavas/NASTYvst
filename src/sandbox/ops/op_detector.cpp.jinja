// op_detector.cpp — Stage-3b C++ sidecar stub for the `detector` op.
// Contract: memory/codegen_design.md § 4. Mirrors op_detector.worklet.js.
// Full-wave rectifier, stateless.
#pragma once
#include <cmath>

namespace shags::ops {

class DetectorOp_{{ node_id }} {
public:
    static constexpr const char* opId = "detector";

    explicit DetectorOp_{{ node_id }}(double sampleRate) : sr_(sampleRate) {}

    void reset() {}

    void setParam(const char* id, double v) {
        // TODO(stage-3b): mode enum ('peak' | 'rms')
    }

    int getLatencySamples() const { return 0; }

    // in:  mono input
    // det: mono control-rate output
    void process(const float* in, float* det, int N) {
        for (int i = 0; i < N; ++i) det[i] = 0.0f;
        // TODO(stage-3b): mode==rms ? x*x : std::fabs(x)
    }

private:
    double sr_;
};

}  // namespace shags::ops
